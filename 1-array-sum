public static Integer arraySum(List<Integer> numbers) {
    if (numbers != null) {
        Integer sum = 0;
        for (Integer i = 0; i < numbers.size(); i++) {
            sum += numbers[i];
        }
        return sum;
    } else {
        return 0;
    }
}