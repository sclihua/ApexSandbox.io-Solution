public Decimal average(List<Decimal> numbers){
    Decimal ave = 0;
    Decimal sum = 0;
    for (Decimal num : numbers) {
        sum += num;
    }
    ave = sum/numbers;
    return Math.round(ave, 2);
}